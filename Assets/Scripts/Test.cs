using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    void Start()
    {
        
    }

    void Update()
    {
        //List<Tuple<int, int>> a = new List<Tuple<int, int>>();
        //Tuple<int, int> b = new Tuple<int, int>(1, 2);
        //a.Add(b);
        //Tuple<int, int> c = new Tuple<int, int>(1, 2);
        //if (a.Contains(c))
        //{
        //    a.Remove(new Tuple<int, int>(1, 2));
        //}
        //Debug.Log(a.Count);

        
    }
}
